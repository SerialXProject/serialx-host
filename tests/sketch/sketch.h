#pragma once

// Firme di setup() e loop(), implementate in sketch.ino.cpp.
// Serve solo al runner desktop (tests/runner/main.cpp) per poterle chiamare
// senza includere direttamente il .cpp dello sketch.
void setup();
void loop();
